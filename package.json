{
  "name": "@instructure/redux-service-middleware",
  "version": "1.0.0",
  "description": "Service middleware for redux",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf lib",
    "build": "babel src/index.js -d lib",
    "test": "jest --coverage",
    "prepublish": "yarn run clean && yarn run build"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.8.3",
    "@babel/core": "^7.8.3",
    "@babel/preset-env": "^7.8.3",
    "jest": "^25.1.0"
  }
}
